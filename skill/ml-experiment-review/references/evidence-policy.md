# Evidence Policy

Evidence is a path, line, artifact metadata/hash, chat declaration, command output, or primary source. Redact secret-like values. Inspect large or binary files using metadata and hashes first. Evidence storage is enabled only for persistent output targets.
